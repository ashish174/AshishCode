package algo.practice.greedy;

public class HuffmanCode {
    /**
     * Huffman Code is a lossless data compression algorithm.
     * Assign some unique code to diff character. The most frequent character gets the smallest code
     * and the least frequent character gets the largest code.
     *
     * We build a tree with all values at leaf. Each left node edge path is marked 0, and right node edge path in marked 1
     *  Left edge = 0
     *  Right edge = 1
     *  Get the path from root to the leaf - this will give unique huffman code for that value
     *
     */
    public static void build(){

    }
}
