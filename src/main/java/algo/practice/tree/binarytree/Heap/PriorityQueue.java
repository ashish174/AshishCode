package algo.practice.tree.binarytree.Heap;

/**
 * Why is Heap Preferred for Priority Queue over BST.
 * Bcoz Heap is implemented using array, and hence there is better locality of reference and hence more cache friendly.
 * Heap does nt require extra space for pointers
 */
public class PriorityQueue {
}
