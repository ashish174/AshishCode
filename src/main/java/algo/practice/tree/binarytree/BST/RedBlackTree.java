package algo.practice.tree.binarytree.BST;

/**
 * A  self-balancing binary search tree where each node has an extra bit,
 * and that bit is often interpreted as the colour (red or black).
 * These colours are used to ensure that the tree remains balanced during insertions and deletions.
 */
public class RedBlackTree {
}
