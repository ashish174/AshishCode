package algo.practice.tree.binarytree;

/**
 * Use 2 stack,
 * 1st stack = primary stack
 * 2nd statck to reverse the elements at each level and store it in 1st stack.
 *
 * 2nd Approach:
 * Use Queue
 * for each level, print level in inverse fashion using stack
 * then process the element in that level in the queue
 */
public class ReverseLevelOrderTraversal {
}
